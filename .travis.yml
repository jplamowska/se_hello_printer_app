language: python
python:
  - "2.7"
install:
  - make deps
script:
  - make lint
  - make test
  - make docker_build
  - make docker run
  - make test_api


after_success:

  - make docker_push



services:
  - docker
deploy:
  provider: heroku
  app: sheltered-atoll-12700
  api_key: ${HEROKU_API_KEY}
